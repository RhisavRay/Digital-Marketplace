"use client" //Added to turn it to a client side component. Bydefault all Next.js components are server side components.

const NavItems = () => {
  return (
    <div>NavItems</div>
  )
}

export default NavItems